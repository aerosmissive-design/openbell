export const GAS_BODY_2A = "function handleSeatReport_(body) {\n  applyLiveConfig_();\n  var expected = String(CONFIG.syncKey || PropertiesService.getScriptProperties().getProperty(\"syncKey\") || \"\").trim();\n  var key = String(body.key || body.token || \"\").trim();\n  if (expected) {\n    if (!key || key !== expected) return jsonOut_({ ok: false, error: \"key\" });\n  }\n  var theaterId = String(body.theaterId || \"\").trim();\n  var allowed = { cgv_yongsan: 1, cgv_yeongdeungpo: 1, megabox_coex: 1, megabox_namyangju: 1 };\n  if (!allowed[theaterId]) return jsonOut_({ ok: false, error: \"unknown theater\" });\n  var incoming = Array.isArray(body.showtimes) ? body.showtimes : [];\n  var cleaned = [];\n  var seenK = {};\n  for (var i = 0; i < incoming.length; i++) {\n    var r = incoming[i] || {};\n    var restSeats = Number(r.restSeats);\n    if (!isFinite(restSeats)) continue;\n    var playDate = String(r.playDate || \"\").trim();\n    var startTime = String(r.startTime || \"\").trim();\n    var hallName = String(r.hallName || \"\").trim();\n    var movieTitle = String(r.movieTitle || \"\").trim();\n    if (!playDate || !startTime || !hallName || !movieTitle) continue;\n    var rk = playDate + \"|\" + startTime + \"|\" + hallName + \"|\" + movieTitle;\n    if (seenK[rk]) continue;\n    seenK[rk] = 1;\n    cleaned.push({\n      playDate: playDate,\n      startTime: startTime,\n      hallName: hallName,\n      movieTitle: movieTitle,\n      movieNo: r.movieNo ? String(r.movieNo) : \"\",\n      bookingUrl: r.bookingUrl ? String(r.bookingUrl).trim() : \"\",\n      scnsNo: r.scnsNo ? String(r.scnsNo).trim() : \"\",\n      scnSseq: r.scnSseq ? String(r.scnSseq).trim() : \"\",\n      restSeats: restSeats,\n      totalSeats: isFinite(Number(r.totalSeats)) ? Number(r.totalSeats) : undefined,\n    });\n    if (cleaned.length >= 5000) break;\n  }\n  if (!cleaned.length) return jsonOut_({ ok: false, error: \"empty payload\" });\n  var source = normalizeReportSource_(body.source);\n  var sk = reportSourceKey_(source, theaterId);\n  var merge = body.mode === \"imax\" || body.mode === \"merge\";\n  var rows = cleaned;\n  if (merge) {\n    var byKey = {};\n    var prev = loadReporterPayload_(PropertiesService.getScriptProperties(), sk);\n    if (prev && Array.isArray(prev.rows)) {\n      var pr = prev.rows;\n      for (var j = 0; j < pr.length; j++) {\n        var prr = pr[j];\n        var pk = String(prr.playDate || \"\") + \"|\" + String(prr.startTime || \"\") + \"|\" + String(prr.hallName || \"\") + \"|\" + String(prr.movieTitle || \"\");\n        byKey[pk] = prr;\n      }\n    }\n    for (var k = 0; k < cleaned.length; k++) {\n      var cr = cleaned[k];\n      var ck = cr.playDate + \"|\" + cr.startTime + \"|\" + cr.hallName + \"|\" + cr.movieTitle;\n      byKey[ck] = cr;\n    }\n    rows = [];\n    for (var bk in byKey) if (byKey.hasOwnProperty(bk)) rows.push(byKey[bk]);\n    if (rows.length > 5000) rows = rows.slice(0, 5000);\n  }\n  var payloadObj = { at: Date.now(), rows: rows, mode: merge ? \"merge\" : \"full\", source: source };\n  var props = PropertiesService.getScriptProperties();\n  try {\n    saveReporterPayload_(props, sk, payloadObj);\n    try { props.setProperty(reportLegacyKey_(theaterId), JSON.stringify({ at: payloadObj.at, source: source, n: rows.length })); } catch (eLeg) {}\n  } catch (eSave) {\n    return jsonOut_({ ok: false, error: \"store \" + String(eSave && eSave.message ? eSave.message : eSave) });\n  }\n  Logger.log(\"seat-report \" + source + \" \" + theaterId + \" \" + rows.length);\n  return jsonOut_({ ok: true, count: cleaned.length, stored: rows.length, merge: merge, source: source, key: sk });\n}\n\n/** G_PC / G_DS423 / G_DS225 \\ub9ac\\ud3ec\\ud2b8\\ub97c live \\ud589\\uc73c\\ub85c \\ud569\\uce68 (\\ubca0\\uc140 nas.server \\uc640 \\ub3d9\\uc77c \\ud0a4) */\nfunction loadReporterLive_() {\n  var out = [];\n  var props = PropertiesService.getScriptProperties();\n  var theaters = CONFIG.theaters || [];\n  var sources = [\"pc\", \"nas423\", \"nas225\"];\n  theaters.forEach(function (theaterId) {\n    var any = false;\n    sources.forEach(function (src) {\n      var parsed = loadReporterPayload_(props, reportSourceKey_(src, theaterId));\n      if (!parsed || !Array.isArray(parsed.rows) || !parsed.at) return;\n      any = true;\n      var label = seatSourceLabel_(src);\n      var chain = String(theaterId).indexOf(\"cgv_\") === 0 ? \"cgv\" : \"megabox\";\n      var theaterName = theaterId;\n      try {\n        if (theaterId === \"cgv_yongsan\") theaterName = \"CGV \\uc6a9\\uc0b0\\uc544\\uc774\\ud30c\\ud06c\\ubab0\";\n        else if (theaterId === \"cgv_yeongdeungpo\") theaterName = \"CGV \\uc601\\ub4f1\\ud3ec\\ud0c0\\uc784\\uc2a4\\ud018\\uc5b4\";\n        else if (theaterId === \"megabox_coex\") theaterName = \"\\uba54\\uac00\\ubc15\\uc2a4 \\ucf54\\uc5d1\\uc2a4\";\n        else if (theaterId === \"megabox_namyangju\") theaterName = \"\\uba54\\uac00\\ubc15\\uc2a4 \\ub0a8\\uc591\\uc8fc\";\n      } catch (e2) {}\n      parsed.rows.forEach(function (r) {\n        var movieTitle = String(r.movieTitle || \"\").trim();\n        var playDate = String(r.playDate || \"\").replace(/-/g, \"\").trim();\n        if (playDate.length === 10) playDate = playDate.replace(/-/g, \"\");\n        var startTime = String(r.startTime || \"\").trim();\n        var hallName = String(r.hallName || \"\").trim() || \"\\uc77c\\ubc18\";\n        var restSeats = Number(r.restSeats);\n        if (!movieTitle || !playDate || !startTime || !isFinite(restSeats)) return;\n        var formats = [];\n        try {\n          if (chain === \"cgv\") formats = cgvFormats_(hallName) || [];\n          else formats = megaFormats_(null, hallName) || [];\n        } catch (e3) {\n          formats = [\"other\"];\n        }\n        if (!formats.length) formats = [\"other\"];\n        var id = chain + \":\" + theaterId + \":\" + playDate + \":\" + startTime + \":\" + hallName + \":\" + movieTitle;\n        var bookUrl = String(r.bookingUrl || \"\").trim();\n        if (!bookUrl) {\n          try {\n            if (chain === \"cgv\") bookUrl = cgvBookUrl_(theaterId, playDate, { movieNo: r.movieNo || \"\", scnsNo: r.scnsNo || \"\", scnSseq: r.scnSseq || \"\" }, null) || \"\";\n            else bookUrl = megaboxUrl_(theaterId === \"megabox_coex\" ? \"1351\" : \"0019\", playDate, r.movieNo || \"\") || \"https://www.megabox.co.kr/booking\";\n          } catch (e5) { bookUrl = \"\"; }\n        }\n        out.push({\n          id: id,\n          theaterId: theaterId,\n          theater: theaterName,\n          title: movieTitle,\n          date: playDate,\n          time: startTime,\n          hall: hallName,\n          formats: formats,\n          restSeats: restSeats,\n          totalSeats: isFinite(Number(r.totalSeats)) ? Number(r.totalSeats) : null,\n          url: bookUrl,\n          seatSource: label,\n          seatLive: true,\n        });\n      });\n    });\n    if (!any) {\n      var leg = props.getProperty(reportLegacyKey_(theaterId)) || \"\";\n      if (!leg) return;\n      try {\n        var p2 = JSON.parse(leg);\n        if (!p2 || !Array.isArray(p2.rows)) return;\n        var src2 = normalizeReportSource_(p2.source);\n        // re-ingest via same loop by writing back to source key once\n        props.setProperty(reportSourceKey_(src2, theaterId), leg);\n      } catch (e4) {}\n    }\n  });\n  return out;\n}\n\nfunction mergeReporterIntoLive_(live) {\n  var rep = loadReporterLive_();\n  if (!rep.length) return live;\n  var byId = {};\n  live.forEach(function (row) {\n    if (row && row.id) byId[row.id] = row;\n  });\n  rep.forEach(function (row) {\n    if (!row || !row.id) return;\n    var prev = byId[row.id];\n    if (!prev) {\n      byId[row.id] = row;\n      live.push(row);\n      return;\n    }\n    // \\ub9ac\\ud3ec\\ud130 \\uc794\\uc5ec\\uc11d\\uc744 \\uc6b0\\uc120 (\\ubca0\\uc140\\uacfc \\ub3d9\\uc77c: NAS/PC \\uac00 \\ucd5c\\uc2e0 \\uc88c\\uc11d)\n    if (typeof row.restSeats === \"number\") {\n      prev.restSeats = row.restSeats;\n      if (row.totalSeats != null) prev.totalSeats = row.totalSeats;\n      prev.seatSource = row.seatSource || prev.seatSource;\n      prev.seatLive = true;\n      if (row.url) prev.url = row.url;\n    }\n  });\n  return live;\n}\n\n\n\nfunction escapeHtml_(s) {\n  return String(s == null ? \"\" : s)\n    .replace(/&/g, \"&amp;\")\n    .replace(/</g, \"&lt;\")\n    .replace(/>/g, \"&gt;\")\n    .replace(/\"/g, \"&quot;\");\n}\n\nfunction reporterSummary_() {\n  var props = PropertiesService.getScriptProperties();\n  var theaters = CONFIG.theaters || [\"cgv_yongsan\", \"cgv_yeongdeungpo\", \"megabox_coex\", \"megabox_namyangju\"];\n  var sources = [\"pc\", \"nas423\", \"nas225\"];\n  var out = [];\n  var now = Date.now();\n  theaters.forEach(function (tid) {\n    sources.forEach(function (src) {\n      var p = loadReporterPayload_(props, reportSourceKey_(src, tid));\n      if (!p) return;\n      try {\n        var at = Number(p.at) || 0;\n        var n = Array.isArray(p.rows) ? p.rows.length : Number(p.n) || 0;\n        out.push({\n          theaterId: tid,\n          source: src,\n          label: seatSourceBoardLabel_(src),\n          at: at,\n          ageMs: at ? now - at : null,\n          count: n,\n        });\n      } catch (e) {}\n    });\n  });\n  return out;\n}\n\nfunction gasBoardHtml_() {\n  applyLiveConfig_();\n  var props = PropertiesService.getScriptProperties();\n  var status = grokStatus_();\n  var shows = [];\n  try {\n    var sc = loadShowcache_();\n    if (Object.prototype.toString.call(sc) === \"[object Array]\") shows = sc;\n    else if (sc && sc.rows) shows = sc.rows;\n  } catch (e0) {\n    try { shows = JSON.parse(props.getProperty(\"showcache\") || \"[]\"); } catch (e1) { shows = []; }\n  }\n  if (!shows) shows = [];\n  var showAt = Number(props.getProperty(\"showcacheAt\") || 0);\n  var reps = reporterSummary_();\n  var bySrc = {};\n  var i;\n  for (i = 0; i < shows.length; i++) {\n    var r0 = shows[i] || {};\n    var s0 = String(r0.seatSource || \"scrape\");\n    s0 = seatSourceBoardLabel_(s0);\n    bySrc[s0] = (bySrc[s0] || 0) + 1;\n  }\n  var stamp = \"\";\n  try { stamp = String(SCRIPT_STAMP || \"\"); } catch (e2) { stamp = \"\"; }\n  var rowsHtml = \"\";\n  var limit = shows.length < 400 ? shows.length : 400;\n  for (i = 0; i < limit; i++) {\n    var r = shows[i] || {};\n    var fmt = \"\";\n    try { fmt = (r.formats || []).join(\",\"); } catch (e3) { fmt = \"\"; }\n    rowsHtml += \"<tr><td>\" + escapeHtml_(r.theater || r.theaterId)\n      + \"</td><td>\" + escapeHtml_(r.title)\n      + \"</td><td>\" + escapeHtml_(r.date) + \" \" + escapeHtml_(r.time)\n      + \"</td><td>\" + escapeHtml_(r.hall)\n      + \"</td><td>\" + escapeHtml_(r.restSeats != null ? r.restSeats : \"-\")\n      + \"</td><td>\" + escapeHtml_(fmt)\n      + \"</td><td>\" + escapeHtml_(seatSourceBoardLabel_(r.seatSource || \"scrape\"))\n      + \"</td></tr>\";\n  }\n  if (!rowsHtml) {\n    rowsHtml = \"<tr><td colspan=7>no data - run install or op=shows</td></tr>\";\n  }\n  var repHtml = \"\";\n  for (i = 0; i < reps.length; i++) {\n    var x = reps[i];\n    var age = \"-\";\n    if (x.ageMs != null) age = Math.round(x.ageMs / 60000) + \"min\";\n    repHtml += \"<tr><td>\" + escapeHtml_(x.label) + \"</td><td>\" + escapeHtml_(x.theaterId)\n      + \"</td><td>\" + x.count + \"</td><td>\" + escapeHtml_(age) + \"</td></tr>\";\n  }\n  if (!repHtml) {\n    repHtml = \"<tr><td colspan=4>no reporter data</td></tr>\";\n  }\n  var srcHtml = \"\";\n  var keys = Object.keys(bySrc);\n  for (i = 0; i < keys.length; i++) {\n    srcHtml += \"<li><b>\" + escapeHtml_(keys[i]) + \"</b>: \" + bySrc[keys[i]] + \"</li>\";\n  }\n  if (!srcHtml) srcHtml = \"<li>empty cache</li>\";\n  var ageGas = \"-\";\n  if (status.gasAgeMs != null) ageGas = Math.round(status.gasAgeMs / 1000) + \"s\";\n  var aliveCls = status.gasAlive ? \"ok\" : \"bad\";\n  var aliveTxt = status.gasAlive ? \" (alive)\" : \" (stale)\";\n  var showAge = showAt ? (Math.round((Date.now() - showAt) / 1000) + \"s ago\") : \"n/a\";\n  var theaters = \"\";\n  try { theaters = (CONFIG.theaters || []).join(\", \"); } catch (e4) { theaters = \"\"; }\n  var html = \"\";\n  html += \"<!DOCTYPE html><html><head><meta charset=utf-8>\";\n  html += \"<meta name=viewport content=\" + \"'\" + \"width=device-width,initial-scale=1\" + \"'\" + \">\";\n  html += \"<title>\\uc624\\ud508\\ubca8 \\uc804\\uad11\\ud310</title><style>\";\n  html += \"body{font-family:system-ui,sans-serif;background:#0b0f14;color:#e8eef6;margin:0;padding:16px}\";\n  html += \"h1{font-size:1.2rem;margin:0 0 8px}h2{font-size:1rem;margin:20px 0 8px;color:#9ecbff}\";\n  html += \".meta{opacity:.85;font-size:.85rem;line-height:1.5}\";\n  html += \"table{border-collapse:collapse;width:100%;font-size:.8rem}\";\n  html += \"th,td{border:1px solid #243044;padding:6px 8px;text-align:left}\";\n  html += \"th{background:#152033}tr:nth-child(even){background:#101820}\";\n  html += \".ok{color:#5dffa8}.bad{color:#ff7b7b}</style></head><body>\";\n  html += \"<h1>\\uc624\\ud508\\ubca8 \\uc804\\uad11\\ud310</h1>\";\n  html += \"<div class=meta>stamp: <b>\" + escapeHtml_(stamp) + \"</b><br>\";\n  html += \"last scan: <span class=\" + aliveCls + \">\" + escapeHtml_(ageGas) + aliveTxt + \"</span><br>\";\n  html += \"showcache: \" + shows.length + \" \\u00b7 \" + escapeHtml_(showAge) + \"<br>\";\n  html += \"theaters: \" + escapeHtml_(theaters) + \"<br>auto refresh 60s</div>\";\n  html += \"<h2>Sources</h2><ul>\" + srcHtml + \"</ul>\";\n";
