import { kstDateKeys, normalizePlayDate } from "@/lib/utils";
import { fetchCgvNaver, fetchCgvOfficialSeatmap, fetchCgvRelaySeatmap, fetchCgvUpcomingCatalog, fetchYongsanTelegram, isCgvId } from "./cgv.server";
import { fetchMegaboxCatalog, fetchMegaboxSchedule, fetchMegaboxSeatmap, fetchNaverMegabox } from "./megabox.server";
import { fetchCgvKtSeatmap } from "./kt.server";
import { readNasSeatmap } from "./nas.server";
import { applyCgvSeatHits, lookupSeatHit, mergeShowtimes, putSeatHit, type SeatHitMap } from "./seats";
import { loadSeatLastKnown, saveSeatLastKnown } from "./seat-cache.server";
import type { RankingMovie, ScanResult, ScanSources, Showtime, TheaterId, TheaterScan } from "./types";
import { normalizeScanSources } from "./types";
import { mergeMovieCatalog, moviesFromShowtimes, titlesMatch } from "./match";

type MegaboxId = "megabox_coex" | "megabox_namyangju";
export type ScanMode = "fast" | "full";
const SCAN_DEADLINES = { fastCatalog: 1500, fastCgvComing: 0, fastNas: 2800, fastTheater: 2500, fastYongsanTele: 1800, lastKnown: 900, catalog: 3000, cgvComing: 3000, gas: 3000, officialCgv: 3500, relay: 4000, nas: 2500, kt: 3500, mega: 4000, theater: 3000, yongsanTele: 2500 } as const;
function deadline<T>(promise: Promise<T>, ms: number, fallback: T): Promise<T> { if (ms <= 0) return Promise.resolve(fallback); return Promise.race([promise.catch(() => fallback), new Promise<T>((resolve) => setTimeout(() => resolve(fallback), ms))]); }

export async function runScan(input: { daysAhead: number; theaters: TheaterId[]; gasWebUrl?: string; sources?: Partial<ScanSources> | null; mode?: ScanMode }): Promise<ScanResult> {
  const mode: ScanMode = input.mode ?? "full"; const fast = mode === "fast"; const days = Math.min(Math.max(input.daysAhead || 7, 1), 30); const playDates = kstDateKeys(days); const wanted = new Set(input.theaters); const sources = normalizeScanSources(input.sources);
  const emptyCatalog = { ranking: [] as RankingMovie[], showing: [] as RankingMovie[], catalog: [] as RankingMovie[] }; const emptyCgv = { map: {} as SeatHitMap, showtimes: [] as Showtime[] }; const emptyNas = { map: {} as SeatHitMap, showtimes: [] as Showtime[], source: "pc" as const };
  const rankingPromise = deadline(fetchMegaboxCatalog(), fast ? SCAN_DEADLINES.fastCatalog : SCAN_DEADLINES.catalog, emptyCatalog); const cgvComingPromise = fast ? Promise.resolve({ movies: [] as RankingMovie[], source: "none" as const }) : deadline(fetchCgvUpcomingCatalog(), SCAN_DEADLINES.cgvComing, { movies: [] as RankingMovie[], source: "none" as const });
  const hasCgv = [...wanted].some(isCgvId); const hasMega = [...wanted].some((id) => id === "megabox_coex" || id === "megabox_namyangju");
  const gasSeats = !fast && input.gasWebUrl ? deadline(loadGasSeatmap(input.gasWebUrl), SCAN_DEADLINES.gas, { status: "timeout" as const, map: {} as GasSeatMap }) : Promise.resolve({ status: "empty" as const, map: {} as GasSeatMap }); const officialCgvSeats = !fast && hasCgv ? deadline(fetchCgvOfficialSeatmap({ days: Math.min(days, 7) }), SCAN_DEADLINES.officialCgv, emptyCgv) : Promise.resolve(emptyCgv); const relaySeats = !fast && hasCgv ? deadline(fetchCgvRelaySeatmap({ days }), SCAN_DEADLINES.relay, emptyCgv) : Promise.resolve(emptyCgv); const nasSeats = deadline(readNasSeatmap([...wanted]), fast ? SCAN_DEADLINES.fastNas : SCAN_DEADLINES.nas, emptyNas); const ktSeats = !fast && hasCgv ? deadline(fetchCgvKtSeatmap({ theaters: [...wanted].filter(isCgvId), dates: playDates }), SCAN_DEADLINES.kt, emptyCgv) : Promise.resolve(emptyCgv); const megaSeats = !fast && hasMega ? deadline(fetchMegaboxSeatmap({ days }), SCAN_DEADLINES.mega, emptyCgv) : Promise.resolve(emptyCgv); const gasShows = !fast && sources.gas && input.gasWebUrl ? deadline(loadGasTimetable(input.gasWebUrl, days), SCAN_DEADLINES.gas, [] as Showtime[]) : Promise.resolve([] as Showtime[]);
  const jobs: Promise<TheaterScan>[] = []; if (wanted.has("megabox_coex")) jobs.push(scanTheater("megabox_coex", playDates, sources, gasShows, mode)); if (wanted.has("megabox_namyangju")) jobs.push(scanTheater("megabox_namyangju", playDates, sources, gasShows, mode)); for (const id of wanted) if (isCgvId(id)) jobs.push(scanTheater(id, playDates, sources, gasShows, mode));
  const [catalog, cgvComing, seats, officialCgv, relay, nas, kt, mega, gasList, ...theaters] = await Promise.all([rankingPromise, cgvComingPromise, gasSeats, officialCgvSeats, relaySeats, nasSeats, ktSeats, megaSeats, gasShows, ...jobs]);
  const gasMap: SeatHitMap = { ...seats.map }; for (const row of gasList) putSeatHit(gasMap, row); const officialMap: SeatHitMap = { ...officialCgv.map, ...mega.map }; const pcMap: SeatHitMap = {}; for (const row of nas.showtimes) if ((row.seatSource ?? "") === "g-pc" || nas.source === "pc") putSeatHit(pcMap, row); const nas225Map: SeatHitMap = nas.source === "nas225" ? { ...nas.map } : {}; const nas423Map: SeatHitMap = nas.source === "nas423" ? { ...nas.map } : {};
  const lastKnown = await deadline(loadSeatLastKnown(), SCAN_DEADLINES.lastKnown, {} as SeatHitMap);
  const withSeats = theaters.map((theater) => { const extraMega = theater.theaterId === "megabox_coex" || theater.theaterId === "megabox_namyangju" ? mega.showtimes.filter((row) => row.theaterId === theater.theaterId) : []; const extraOfficialCgv = isCgvId(theater.theaterId) ? officialCgv.showtimes.filter((row) => row.theaterId === theater.theaterId) : []; const extraRelay = isCgvId(theater.theaterId) ? relay.showtimes.filter((row) => row.theaterId === theater.theaterId) : []; const extraNas = nas.showtimes.filter((row) => row.theaterId === theater.theaterId); const extraKt = isCgvId(theater.theaterId) ? kt.showtimes.filter((row) => row.theaterId === theater.theaterId) : []; const extraGas = gasList.filter((row) => row.theaterId === theater.theaterId); const extraOfficial = [...extraMega, ...extraOfficialCgv]; const extra = [...extraGas, ...extraRelay, ...extraKt, ...extraNas, ...extraOfficial]; const base = normalizeShowtimes(theater.showtimes); const normalizedExtra = normalizeShowtimes(extra); const merged = normalizedExtra.length ? mergeShowtimes(base, normalizedExtra) : base; let live = applySeatLayers(merged, [{ map: officialMap, source: "official" }, { map: pcMap, source: "g-pc" }, { map: nas225Map, source: "g-nas225+" }, { map: nas423Map, source: "g-nas423+" }, { map: kt.map, source: "cgv-kt" }, { map: relay.map, source: "cgv-relay" }, { map: gasMap, source: "gas-cache" }]); if (extraNas.length) live = applyReporterFallback(live, extraNas); const showtimes = applyCgvSeatHits(live, lastKnown, false, true).map((row) => ({ ...row, playDate: normalizePlayDate(row.playDate), seatSource: row.seatSource ?? (row.restSeats != null ? "last-known" : undefined) })); let source = theater.source; if (!theater.showtimes.length) { if (extraOfficial.length) source = "official"; else if (extraNas.length) source = "nas-report"; else if (extraKt.length) source = "cgv-kt"; else if (extraRelay.length) source = "cgv-relay"; else if (extraGas.length) source = "gas-cache"; } return { ...theater, showtimes, source, ok: theater.ok || showtimes.length > 0, error: showtimes.length ? null : theater.error }; });
  const tagged = withSeats.map((theater) => ({ ...theater, seatSource: detectSeatSource(theater.showtimes) })); const harvested: SeatHitMap = {}; for (const theater of tagged) for (const row of theater.showtimes) putSeatHit(harvested, row); if (Object.keys(harvested).length) await saveSeatLastKnown(harvested); const ranking = catalog.ranking.length ? catalog.ranking : rankingFromShows(tagged); const showing = catalog.showing.length ? catalog.showing : ranking;
  const seatSourceTimes = Object.fromEntries(tagged.map((theater) => [theater.theaterId, latestSeatSourceTimes(theater.theaterId, [{ key: "official", map: officialMap }, { key: "g-pc", map: pcMap, source: "pc" }, { key: "g-nas225+", map: nas225Map, source: "nas225" }, { key: "g-nas423+", map: nas423Map, source: "nas423" }, { key: "cgv-kt", map: kt.map }, { key: "cgv-relay", map: relay.map }, { key: "gas-cache", map: gasMap }])]));
  return { scannedAt: new Date().toISOString(), playDates, ranking, showing, catalog: mergeMovieCatalog(catalog.catalog ?? [], catalog.ranking, catalog.showing, cgvComing.movies, moviesFromShowtimes(tagged.flatMap((t) => t.showtimes))), theaters: tagged, seatSourceTimes };
}
function normalizeShowtimes(rows: Showtime[]): Showtime[] { return rows.map((row) => ({ ...row, playDate: normalizePlayDate(row.playDate) })); }
async function scanTheater(theaterId: TheaterId, playDates: string[], sources: ScanSources, gasShows: Promise<Showtime[]>, mode: ScanMode): Promise<TheaterScan> { const fast = mode === "fast"; const naverPromise = sources.naver ? deadline(fetchNaver(theaterId), fast ? SCAN_DEADLINES.fastTheater : SCAN_DEADLINES.theater, new Map<string, Showtime[]>()) : Promise.resolve(new Map<string, Showtime[]>()); const officialByDate = new Map<string, Showtime[]>(); const officialPromise = !fast && !isCgvId(theaterId) && sources.official ? Promise.all(playDates.map(async (date) => [date, await deadline(fetchMegaboxSchedule(theaterId as MegaboxId, date, { ignoreCircuit: true, timeoutMs: 8000 }), SCAN_DEADLINES.mega, [] as Showtime[])] as const)) : Promise.resolve([] as Array<readonly [string, Showtime[]]>); const telePromise = theaterId === "cgv_yongsan" ? deadline(fetchYongsanTelegram(), fast ? SCAN_DEADLINES.fastYongsanTele : SCAN_DEADLINES.yongsanTele, new Map<string, Showtime[]>()) : Promise.resolve(new Map<string, Showtime[]>()); const [naverByDate, officialRows, teleByDate] = await Promise.all([naverPromise, officialPromise, telePromise]); for (const [date, rows] of officialRows) if (rows.length) officialByDate.set(date, rows); const stillMissing = playDates.filter((d) => !(officialByDate.get(d)?.length || naverByDate.get(d)?.length)); let gasByDate = new Map<string, Showtime[]>(); if (!fast && sources.gas) { try { gasByDate = byDateForTheater(await gasShows, theaterId); } catch { gasByDate = new Map(); } } const showtimes: Showtime[] = []; let usedOfficial = false, usedNaver = false, usedGas = false, usedTele = false; for (const date of playDates) { const official = officialByDate.get(date) ?? []; const naver = naverByDate.get(date) ?? []; const primary = official.length ? official : naver; if (primary.length) { showtimes.push(...primary); if (officialByDate.get(date)?.length) usedOfficial = true; if (naverByDate.get(date)?.length) usedNaver = true; continue; } if (stillMissing.includes(date)) { const tele = teleByDate.get(date); if (tele?.length) { showtimes.push(...tele); usedTele = true; continue; } } const gas = gasByDate.get(date); if (gas?.length) { showtimes.push(...gas); usedGas = true; } } const source = usedOfficial ? "official" : usedNaver ? "naver-place" : usedTele ? "yongsan-channel" : usedGas ? "gas-cache" : "none"; if (!showtimes.length) return { theaterId, ok: false, error: failMessage(theaterId, sources), showtimes: [], source, seatSource: "none" }; return { theaterId, ok: true, error: null, showtimes: normalizeShowtimes(showtimes), source, seatSource: "none" }; }
async function fetchNaver(theaterId: TheaterId): Promise<Map<string, Showtime[]>> { if (isCgvId(theaterId)) return fetchCgvNaver(theaterId); return fetchNaverMegabox(theaterId as MegaboxId); }
function failMessage(theaterId: TheaterId, sources: ScanSources) { const tried = isCgvId(theaterId) ? [sources.naver ? "네이버" : "", theaterId === "cgv_yongsan" ? "용아맥 채널" : ""].filter(Boolean) : [sources.official ? "공홈" : "", sources.naver ? "네이버" : ""].filter(Boolean); const name = isCgvId(theaterId) ? "CGV" : "메가박스"; return `${name} 시간표를 ${tried.join(" → ") || "조회"}에서 가져오지 못했습니다.`; }
function rankingFromShows(theaters: TheaterScan[]): RankingMovie[] { const counts = new Map<string, number>(); for (const theater of theaters) for (const show of theater.showtimes) { const title = show.movieTitle.trim(); if (title) counts.set(title, (counts.get(title) ?? 0) + 1); } return [...counts.entries()].sort((a, b) => b[1] - a[1]).slice(0, 9).map(([title], i) => ({ rank: i + 1, title, movieNo: "", bookingRate: null, posterUrl: null, releaseDate: null, bookingOpen: true, released: true })); }
type GasSeatMap = SeatHitMap; type GasShowRow = { id?: string; theaterId?: string; theater?: string; title?: string; date?: string; time?: string; hall?: string; formats?: Showtime["formats"]; restSeats?: number | null; totalSeats?: number | null; url?: string };
function stampSeatMap(map: SeatHitMap, at: number): SeatHitMap { if (!Object.keys(map).length || !at) return map; return Object.fromEntries(Object.entries(map).map(([key, hit]) => [key, { ...hit, at: typeof hit.at === "number" && Number.isFinite(hit.at) ? hit.at : at }])); }
function latestSeatSourceTimes(theaterId: TheaterId, sources: { key: string; map: SeatHitMap; source?: string }[]) { const out: Record<string, string> = {}; const siteNo = ({ cgv_yongsan: "0013", cgv_yeongdeungpo: "0059", megabox_coex: "1351", megabox_namyangju: "0019" } as Record<string, string>)[theaterId] ?? ""; const prefixes = siteNo ? [`k:${siteNo}|`, `m:${siteNo}|`] : []; for (const source of sources) { let latest = 0; for (const [key, hit] of Object.entries(source.map)) { if (!hit || typeof hit.at !== "number" || !Number.isFinite(hit.at)) continue; if (source.source && hit.source !== source.source) continue; if (!prefixes.some((prefix) => key.startsWith(prefix))) continue; latest = Math.max(latest, hit.at); } if (latest > 0) out[source.key] = new Date(latest).toISOString(); } return out; }
function applySeatLayers(rows: Showtime[], layers: { map: SeatHitMap; source: string }[]): Showtime[] { const newest: SeatHitMap = {}; const sourceByKey = new Map<string, string>(); for (const { map, source } of layers) for (const [key, hit] of Object.entries(map)) { if (typeof hit.rest !== "number" || !Number.isFinite(hit.rest)) continue; const at = typeof hit.at === "number" && Number.isFinite(hit.at) ? hit.at : 0; const prev = newest[key]; if (!prev || (prev.at ?? 0) <= at) { newest[key] = { ...hit, at }; sourceByKey.set(key, source); } } return applyCgvSeatHits(rows, newest, true, true).map((row) => { const hit = lookupSeatHit(row, newest); if (!hit) return row; const source = Object.keys(newest).filter((key) => newest[key].at === hit.at).map((key) => sourceByKey.get(key)).find(Boolean); return { ...row, seatSource: source ?? row.seatSource, seatCheckedAt: hit.at ? new Date(hit.at).toISOString() : row.seatCheckedAt }; }); }
function applyReporterFallback(rows: Showtime[], reporter: Showtime[]): Showtime[] { return rows.map((row) => { if (row.restSeats != null) return row; const hit = reporter.find((candidate) => candidate.theaterId === row.theaterId && normalizePlayDate(candidate.playDate) === normalizePlayDate(row.playDate) && normTime(candidate.startTime) === normTime(row.startTime) && hallsMatch(candidate.hallName, row.hallName) && (titlesMatch(candidate.movieTitle, row.movieTitle) || Boolean(candidate.movieNo && row.movieNo && candidate.movieNo === row.movieNo))); if (!hit || hit.restSeats == null) return row; return { ...row, restSeats: hit.restSeats, totalSeats: hit.totalSeats ?? row.totalSeats, seatLive: true, seatCheckedAt: hit.seatCheckedAt ?? row.seatCheckedAt, seatSource: hit.seatSource ?? (hit.theaterId.startsWith("cgv") ? "g-pc" : row.seatSource) }; }); }
function hallsMatch(a: string, b: string): boolean { const normalize = (value: string) => String(value || "").toUpperCase().replace(/[\s|()[\]{}·•]/g, ""); const variants = (value: string) => { const base = normalize(value); const noBracket = base.replace(/\[.*?\]/g, ""); const loose = noBracket.replace(/관$/, ""); return new Set([base, noBracket, loose, `${loose}관`].filter(Boolean)); }; const aa = variants(a); const bb = variants(b); if (!aa.size || !bb.size) return false; for (const v of aa) if (bb.has(v)) return true; return false; }
function normTime(time: string) { const digits = String(time || "").replace(/\D/g, ""); if (digits.length < 3) return String(time || "").trim(); const padded = digits.length === 3 ? `0${digits}` : digits.slice(0, 4); return `${padded.slice(0, 2)}:${padded.slice(-2)}`; }
function detectSeatSource(rows: Showtime[]) { const live = rows.filter((row) => row.restSeats != null && row.seatLive !== false); if (!live.length) return "none"; const latest = live.filter((row) => row.seatSource && row.seatSource !== "none" && row.seatCheckedAt).sort((a, b) => new Date(b.seatCheckedAt!).getTime() - new Date(a.seatCheckedAt!).getTime())[0]; return latest?.seatSource ?? "none"; }
function byDateForTheater(shows: Showtime[], theaterId: TheaterId): Map<string, Showtime[]> { const map = new Map<string, Showtime[]>(); for (const show of shows) { if (show.theaterId !== theaterId) continue; const normalized = normalizePlayDate(show.playDate); const list = map.get(normalized) ?? []; list.push({ ...show, playDate: normalized }); map.set(normalized, list); } return map; }
export async function pingSeatmap(input: { url?: string; fresh?: boolean; theaterId?: TheaterId; daysAhead?: number }) { const days = Math.min(Math.max(input.daysAhead ?? 7, 1), 14); const fresh = Boolean(input.fresh); const wantMega = !input.theaterId || input.theaterId === "megabox_coex" || input.theaterId === "megabox_namyangju"; const wantCgv = !input.theaterId || input.theaterId === "cgv_yongsan" || input.theaterId === "cgv_yeongdeungpo"; const megaPromise = wantMega ? fetchMegaboxSeatmap({ theaterId: input.theaterId === "megabox_coex" || input.theaterId === "megabox_namyangju" ? input.theaterId : undefined, days: Math.min(days, 5), fresh }).catch(() => ({ map: {} as SeatHitMap, showtimes: [] as Showtime[] })) : Promise.resolve({ map: {} as SeatHitMap, showtimes: [] as Showtime[] }); const officialCgvPromise = wantCgv ? fetchCgvOfficialSeatmap({ theaterId: input.theaterId === "cgv_yongsan" || input.theaterId === "cgv_yeongdeungpo" ? input.theaterId : undefined, days: Math.min(days, 7), fresh }).catch(() => ({ map: {} as SeatHitMap, showtimes: [] as Showtime[] })) : Promise.resolve({ map: {} as SeatHitMap, showtimes: [] as Showtime[] }); const [mega, officialCgv] = await Promise.all([megaPromise, officialCgvPromise]); const megaHit = Object.keys(mega.map).length > 0 || mega.showtimes.some((row) => typeof row.restSeats === "number"); const cgvOfficialHit = Object.keys(officialCgv.map).length > 0 || officialCgv.showtimes.some((row) => typeof row.restSeats === "number"); const relay = wantCgv && !cgvOfficialHit ? await fetchCgvRelaySeatmap({ theaterId: input.theaterId === "cgv_yongsan" || input.theaterId === "cgv_yeongdeungpo" ? input.theaterId : undefined, days, fresh }).catch(() => ({ map: {} as SeatHitMap, showtimes: [] as Showtime[] })) : { map: {}, showtimes: [] as Showtime[] }; let gasMap: SeatHitMap = {}; let gasShows: Showtime[] = []; let gasStatus: "ok" | "old" | "denied" | "empty" | "badurl" | "timeout" = "empty"; if (input.url?.trim() && ((wantMega && !megaHit) || (wantCgv && !cgvOfficialHit))) { const live = await loadGasTimetable(input.url.trim(), days, input.theaterId).catch(() => [] as Showtime[]); if (live.length) { gasShows = live; for (const row of live) putSeatHit(gasMap, row); gasStatus = "ok"; } else { const gas = await loadGasSeatmap(input.url, fresh); gasMap = gas.map; gasStatus = gas.status; } } const map = { ...gasMap, ...relay.map, ...officialCgv.map, ...mega.map }; const extraShows = [...gasShows, ...relay.showtimes, ...officialCgv.showtimes, ...mega.showtimes]; const keys = Object.keys(map); const cgvCount = keys.filter((key) => key.startsWith("k:") || key.startsWith("cgv:")).length; const seatSourceTimes: Record<string, Record<string, string>> = {}; for (const theaterId of ["megabox_coex", "megabox_namyangju", "cgv_yongsan", "cgv_yeongdeungpo"] as TheaterId[]) seatSourceTimes[theaterId] = latestSeatSourceTimes(theaterId, [{ key: "official", map: { ...officialCgv.map, ...mega.map } }, { key: "relay", map: relay.map }, { key: "gas", map: gasMap }]); const status = keys.length || extraShows.length ? ("ok" as const) : gasStatus; return { status, count: keys.length || extraShows.length, cgvCount, map, showtimes: extraShows, seatSourceTimes }; }
async function loadGasTimetable(url: string, days: number, theaterId?: TheaterId): Promise<Showtime[]> { const params: Record<string, string> = { op: "live", days: String(Math.min(days, 30)) }; if (theaterId) params.theater = theaterId; const live = await loadGasJson(url, params); if (live.length) return live; const mega = await loadGasJson(url, { ...params, op: "mega" }); if (mega.length) return mega; return loadGasShows(url); }
async function loadGasJson(url: string, params: Record<string, string>): Promise<Showtime[]> { let target: URL; try { target = new URL(url.trim()); } catch { return []; } const host = target.hostname; if (!host.endsWith("script.google.com") && !host.endsWith("googleusercontent.com")) return []; for (const [key, value] of Object.entries(params)) target.searchParams.set(key, value); try { const res = await fetch(target.toString(), { redirect: "follow", signal: AbortSignal.timeout(25000) }); const text = await res.text(); if (!text || /sign in|accounts\.google/i.test(text)) return []; const start = text.indexOf("["); const end = text.lastIndexOf("]"); if (start < 0 || end <= start) return []; const json = JSON.parse(text.slice(start, end + 1)) as GasShowRow[]; if (!Array.isArray(json)) return []; return json.map(gasRowToShowtime).filter((row): row is Showtime => Boolean(row)); } catch { return []; } }
async function loadGasShows(url: string): Promise<Showtime[]> { return loadGasJson(url, { op: "shows" }); }
function gasRowToShowtime(row: GasShowRow): Showtime | null { const theaterId = row.theaterId as TheaterId | undefined; const playDate = normalizePlayDate(String(row.date || "")); const startTime = String(row.time || ""); const title = String(row.title || "").trim(); if (!theaterId || !playDate || !startTime || !title || !["megabox_coex", "megabox_namyangju", "cgv_yongsan", "cgv_yeongdeungpo"].includes(theaterId)) return null; const chain = theaterId.startsWith("cgv_") ? "cgv" : "megabox"; return { id: row.id || `${chain}:${theaterId}:${playDate}:${startTime}:${row.hall || ""}:${title}`, theaterId, theaterName: String(row.theater || "").replace(/^메가박스\s*|^CGV\s*/, "") || theaterId, chain, movieTitle: title, movieNo: "", playDate, startTime, endTime: null, hallName: String(row.hall || ""), formats: Array.isArray(row.formats) ? row.formats : [], restSeats: typeof row.restSeats === "number" ? row.restSeats : null, totalSeats: typeof row.totalSeats === "number" ? row.totalSeats : null, bookingUrl: String(row.url || ""), bookable: true }; }
async function loadGasSeatmap(url: string, fresh = false): Promise<{ status: "ok" | "old" | "denied" | "empty" | "badurl" | "timeout"; map: GasSeatMap }> { let target: URL; try { target = new URL(url.trim()); } catch { return { status: "badurl", map: {} }; } const host = target.hostname; if (!host.endsWith("script.google.com") && !host.endsWith("googleusercontent.com")) return { status: "badurl", map: {} }; target.searchParams.set("op", "seatmap"); if (fresh) target.searchParams.set("fresh", "1"); try { const res = await fetch(target.toString(), { redirect: "follow", signal: AbortSignal.timeout(fresh ? 20000 : 15000) }); const text = await res.text(); if (!text || /sign in|accounts\.google/i.test(text)) return { status: "denied", map: {} }; const start = text.indexOf("{"); const end = text.lastIndexOf("}"); if (start < 0 || end <= start) return { status: "old", map: {} }; const json = JSON.parse(text.slice(start, end + 1)) as GasSeatMap; if (!json || typeof json !== "object") return { status: "old", map: {} }; const cgvCount = Object.keys(json).filter((key) => key.startsWith("k:") || key.startsWith("cgv:")).length; if (!Object.keys(json).length) return { status: "empty", map: {} }; if (fresh && !cgvCount) return { status: "empty", map: json }; return { status: "ok", map: json }; } catch (err) { const msg = err instanceof Error ? err.message : ""; if (/abort|timeout/i.test(msg)) return { status: "timeout", map: {} }; return { status: "old", map: {} }; } }
