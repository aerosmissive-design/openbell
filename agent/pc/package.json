{
  "name": "openbell-pc-agent",
  "version": "2.0.2",
  "private": true,
  "type": "module",
  "description": "Standalone Windows PC CGV booking agent for OpenBell (hard-stop before payment)",
  "scripts": {
    "start": "tsx src/run.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/seat-ranker.test.ts"
  },
  "dependencies": {
    "playwright": "^1.62.0"
  },
  "devDependencies": {
    "@types/node": "^22.16.5",
    "tsx": "^4.20.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
