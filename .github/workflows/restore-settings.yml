name: restore-settings
on:
  workflow_dispatch:
jobs:
  restore:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Skip auto restore
        run: echo "settings-view is committed directly; z64 restore is disabled."
